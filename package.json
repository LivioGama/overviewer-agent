{
  "name": "overviewer-agent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/shared"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
